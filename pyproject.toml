[project]
name = "codex-driven-dev-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "pseuriah", email = "pariahpseudo@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
codex-driven-dev-platform = "codex_driven_dev_platform:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
